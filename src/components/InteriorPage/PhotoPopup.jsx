import React, {useState} from "react";

export default function PhotoPopup(isInitallyOpen, photoToShow) {
	const [isOpen, setIsOpen] = useState(isInitallyOpen);

	return <div className="bg-black-500">

	</div>;
}
